import { chatRepository } from "../libs/app/repository/index";
import {addUser_useCase,} from "../libs/useCase";

const useCase:any={
  
    addUser_useCase,
    
    
}

const repository:any = {
    chatRepository
} 

export default {useCase,repository}